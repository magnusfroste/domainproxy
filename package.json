{
  "name": "subdomino",
  "version": "1.0.0",
  "description": "Multi-tenant subdomain proxy service (Subdomino) - API for developers to register custom subdomains that proxy to their backends",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "fs-extra": "^11.1.1",
    "sqlite3": "^5.1.7",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}